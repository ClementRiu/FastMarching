#include "priorite.h"

// Ajoute un element
void FilePriorite::push(PointDist d) {
    // A completer
}

// Retire un element
PointDist FilePriorite::pop() {
    // A completer
    return v.back();
}
